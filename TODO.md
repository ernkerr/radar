# Radar — TODO

## Supabase Setup

- [ ] Create a new Supabase project at [supabase.com](https://supabase.com)
- [ ] Run `supabase/migrations/001_initial_schema.sql` in the SQL editor
- [ ] Run `supabase/migrations/002_rls_policies.sql` in the SQL editor
- [ ] Copy project URL + anon key into `apps/web/.env.local` (see `apps/web/.env.local.example`)
- [ ] Copy project URL + service role key into `services/workers/.env` (see `services/workers/.env.example`)
